# VPS 上复制为 .env 并填值（.env 已被 .gitignore 排除，不会进仓库）
#
# 数据库密码：首次启动时写入数据卷，之后修改需一并改库内密码，否则连不上
POSTGRES_PASSWORD=

# Cloudflare Tunnel 令牌
# 取得方式：Cloudflare 控制台 → Zero Trust → Networks → Tunnels → 新建隧道
# 选 Docker 安装方式后，命令里 --token 后面那一长串即是
CF_TUNNEL_TOKEN=
